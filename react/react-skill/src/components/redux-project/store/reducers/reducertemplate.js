/**  reducer 模版 */

const initial = {};
export default function voteReducer(state = initial, action) {
  const nextState = { ...state };
  switch (action.type) {
    // case value:
    //   break;

    default:
      break;
  }
  return nextState;
}
